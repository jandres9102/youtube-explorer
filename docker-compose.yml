version: '2'

services:
  db : 
    image: mongo
    restart: always 
    ports: 
    - 27017:27017
    volumes:
    - ./data:/data/db
    
  python_app: 
    build :
      context : .
      dockerfile : Dockerfile
    volumes: 
    - ./video:/work/video
    depends_on:
      - db
    
